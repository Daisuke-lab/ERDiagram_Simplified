import { RootState } from "../../../store/store";


// export function getCurrentTable (state:RootState) {
//     const currentSelection = state.commons.currentSelection
//     const currentTable = state.tables.tables.find((table) => table.id === currentSelection?.id)
//     return currentTable
// }

// export function getCurrentSimpleText (state:RootState) {
//     const currentSelection = state.commons.currentSelection
//     const currentSimpleText = state.simpleTexts.simpleTexts.find((simpleText) => simpleText.id === currentSelection?.id)
//     return currentSimpleText
// }